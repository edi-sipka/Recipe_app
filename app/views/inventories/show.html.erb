
<div class="w-1/2 m-auto text-sm flex flex-col gap-8">
  <h1 class="text-4xl font-bold text-center"><%= @inventory.name %></h1>

  <div class="flex justify-between">
    <button type="button" class="self-end text-indigo-600 text-xs border-black border-2 py-2 px-4 hover:text-indigo-500">
      <%= link_to "Back", :back%>
    </button>
    <button type="button" class="self-end text-indigo-600 text-xs border-black border-2 py-2 px-4 hover:text-indigo-500">
      <%= link_to "Add Food", new_inventory_inventory_food_path(params[:id]) %>
    </button>
  </div>
</div>

<hr class="my-4 mx-auto h-1 w-1/2 rounded border-0 md:mb-10 md:mt-4 bg-black">

<%= render partial: 'inventory_foods/inventory_food_lists', locals: {inventory: @inventory} %>
